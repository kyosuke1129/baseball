
<h1>
  <%= @team.key %>のページ<br>
</h1>
<h4>
  選手一覧
</h4>
   

   <% @players.each do |player| %>
    <p>
      <%= player.number %>     <%= player.name %>  <%= player.at_bat %>    <%= player.hand %>

    </p>

    <% end %>

    <%= link_to "選手登録", new_team_player_path(@team) %>
    <%= link_to "編集：成績入力", edit_team_player_path(@team) %>


