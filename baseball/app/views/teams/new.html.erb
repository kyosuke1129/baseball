<%= form_for @team do |f| %>
    <h3>
      登録
    </h3>
     チーム名：<%= f.text_field :key, placeholder: "チーム名：", class: "form-control" %><br>
     

      <%= f.submit "送信", class: "btn btn-primary withripple" %>
<% end %>
